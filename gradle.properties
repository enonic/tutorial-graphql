# Settings for publishing to a Maven repo
group = com.enonic.tutorial

# Gradle Project settings
projectName = tutorial-graphql
version = 1.0.0-SNAPSHOT

# XP App values
appName = com.enonic.tutorial.graphql

# Min required version of XP
xpVersion=8.0.4
